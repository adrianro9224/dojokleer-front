import { RegisterAssessmentAnswerForm } from './register-assessment-answer-form';

describe('RegisterAssessmentAnswerForm', () => {
  it('should create an instance', () => {
    expect(new RegisterAssessmentAnswerForm()).toBeTruthy();
  });
});
